name: Build & Release nano-zyrkel

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libssl-dev pkg-config

      - name: Build binary
        run: cargo build --release

      - name: Create checksum
        run: |
          cd target/release
          sha256sum nano-zyrkel > nano-zyrkel-linux.sha256
          cp nano-zyrkel nano-zyrkel-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nano-zyrkel-linux
          path: |
            target/release/nano-zyrkel-linux
            target/release/nano-zyrkel-linux.sha256

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            target/release/nano-zyrkel-linux
            target/release/nano-zyrkel-linux.sha256
