# theme-minimal

Black on white, Helvetica, no chrome. Use it for status pages and
short reports where the data should be the only thing on screen.

## Use it

```bash
cp -r theme-minimal/docs/* my-nano-zyrkel/docs/
```

Recommended `wasm-core` profile: `data` only — this theme does not
draw any charts.
